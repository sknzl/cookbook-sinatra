<h1> List of Swen's recipes </h1>

<ul>
  <% @recipes.each_with_index do |element, index| %>
    <li>
      <h3> <%= element.name %> </h3>
      <p> <%= element.description %> </p>
      <a href="/delete/<%= index %>">Destroy</a>
    </li>
  <% end %>
</ul>
<a href="/new">Create a new recipe</a>
